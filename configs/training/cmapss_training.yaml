experiment_name: "cmapss_baseline"
log_dir: "logs/"
dataset_path: "tests/fixtures/cmapss_fd001_sample.npz"
dataset_name: "cmapss"

batch_size: 4
epochs: 5
learning_rate: 0.001
weight_decay: 0.0001
clip_grad: 1.0

loss_weights:
  rul: 1.0
  fault: 0.0 # Disabled
  anomaly: 0.0 # Disabled

modality_dropout_p: 0.1

# TODO(Naman Data): These stats were computed from a 20-sample toy fixture!
# MUST be recomputed from the real training split once it exists.
target_normalization:
  rul_mean: 219.5
  rul_std: 28.8
